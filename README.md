# Borrow-Rates

```
# Install dependencies
apt-get update
apt install python3 python3-venv
```

```
# Change directory into the folder
cd /Speculation_Index

# Create/intialise the virtual environment
python3 -m venv env

source env/bin/activate

# Install dependencies
pip install -r requirements.txt

# To test

python3 app.py
```
